rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return signedIn() && request.auth.uid == userId;
    }

    function publicProfileOwner(username) {
      return signedIn()
        && get(/databases/$(database)/documents/publicProfiles/$(username)).data.uid == request.auth.uid;
    }

    match /users/{userId} {
      allow read: if signedIn();
      allow create, update, delete: if isOwner(userId);

      match /locations/{locationId} {
        allow read: if signedIn();
        allow create, update, delete: if isOwner(userId);

        match /images/{imageId} {
          allow read: if signedIn();
          allow create, update, delete: if isOwner(userId);
        }

        match /meta/{metaId} {
          allow read: if signedIn();
          allow create, update, delete: if isOwner(userId);
        }
      }
    }

    match /usernames/{username} {
      allow read: if true;
      allow create: if signedIn() && request.resource.data.uid == request.auth.uid;
      allow update: if signedIn()
        && resource.data.uid == request.auth.uid
        && request.resource.data.uid == request.auth.uid;
      allow delete: if signedIn() && resource.data.uid == request.auth.uid;
    }

    match /publicProfiles/{username} {
      allow read: if true;
      allow create: if signedIn() && request.resource.data.uid == request.auth.uid;
      allow update: if signedIn()
        && resource.data.uid == request.auth.uid
        && request.resource.data.uid == request.auth.uid;
      allow delete: if signedIn() && resource.data.uid == request.auth.uid;

      match /topImages/{imageId} {
        allow read: if true;
        allow create, update: if publicProfileOwner(username)
          && request.resource.data.ownerUid == request.auth.uid;
        allow delete: if publicProfileOwner(username);
      }
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
